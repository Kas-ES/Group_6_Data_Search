# Data Search Team

In order to setup the project locally, first install dependencies by running

```
npm install
```

Afterwards the code base can be compiled and server started by running

```
npm start
```

## Debugging

To enable server-side logging of requests and other debugging features, enable debugging in `src/config/service.ts`.
