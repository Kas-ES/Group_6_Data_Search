import { Index } from './interfaces/Index';
import { Result } from './interfaces/Result';
export default class Search {
    private static searchByWord;
    static perform(index: Index, query: string): Result[];
}
