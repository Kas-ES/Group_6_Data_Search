export default {
  /**
   * Number of results returned through the api
   */
  maxResults: 10,

  /**
   * Scoring multiplier used for exact match words
   */
  wordScoreMultiplier: 0.4,

  /**
   * Scoring multiplier used for fuzzy words
   */
  fuzzyScoreMultiplier: 0.8,
};
